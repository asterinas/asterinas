# SPDX-License-Identifier: MPL-2.0

export FSTYP=ext2

# The xfstests test/scratch images are attached by qemu_args.sh as the 3rd and
# 4th virtio-blk devices, which Linux names /dev/vdc and /dev/vdd respectively
# (after /dev/vda=ext2 and /dev/vdb=exfat).
export TEST_DEV=${XFSTESTS_TEST_DEV:-/dev/vdc}
export SCRATCH_DEV=${XFSTESTS_SCRATCH_DEV:-/dev/vdd}

export TEST_DIR=/opt/xfstests/test
export SCRATCH_MNT=/opt/xfstests/scratch

export MOUNT_OPTIONS="-o noacl"
export EXT_MOUNT_OPTIONS="-o noacl"

export FSTYP_HAS_NON_DEFAULT_OPTS=0
export SELINUX_MOUNT_OPTIONS=" "

export MKFS_OPTIONS="-F"
